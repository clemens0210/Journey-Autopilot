# Google Agent Development Kit (2.x).
# Version gepinnt, damit alle im Team dieselbe nutzen.
# Aktuelle 2.x-Version ggf. auf https://pypi.org/project/google-adk/ prüfen
# und hier anpassen.
#
# [extensions] zieht LiteLLM mit rein — nötig für das Uni-Köln-GPT-Backend
# (OpenAI-kompatibler Endpunkt).
google-adk[extensions]>=2.0,<3.0

# HTTP-Client für den db-vendo-client-Sidecar (siehe db_service/ und db_api.py).
requests>=2.31

# Onboarding-Web-App (onboarding/): JSON-API + DB-Navigator-Style-UI.
fastapi>=0.110
uvicorn>=0.29

# Optional, NUR zum (Neu-)Bauen der historischen Verspätungs-Referenz mit
# scripts/build_db_delay_reference.py (Parquet von Hugging Face verdichten).
# Die Laufzeit braucht das nicht — sie liest nur die fertige JSON.
#   pip install pyarrow huggingface_hub
